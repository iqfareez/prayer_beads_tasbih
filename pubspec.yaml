name: prayer_beads
description: Tasbih Zikr

publish_to: "none"

version: 1.2.2+6

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  animated_flip_counter: ^0.3.4
  carousel_slider: ^5.0.0
  cupertino_icons: ^1.0.1
  firebase_core: ^3.13.0
  share_plus: ^11.0.0
  shared_preferences: ^2.5.3
  signals: ^6.0.2
  url_launcher: ^6.0.3
  vibration: ^3.1.3

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/zikr/
    - assets/
